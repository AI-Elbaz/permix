```tsx
// Setup wherever you want
import { permix } from './permix'

const user = await fetchUser()

permix.setup({
  post: {
    create: true,
    edit: post => !post.published,
    delete: user.role === 'admin',
  },
})
```
